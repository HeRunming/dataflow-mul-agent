# Run from this repository root. Installs the DataFlow submodule that
# DATAFLOW_ROOT also points at, so the catalog, source previews and
# execution all read the same checkout.
-e external/DataFlow
-e .[web,test]
